#include "main.h"

/**
 * _abs - compitrs absolutr integer
 * @n: n is an integer
 *
 * Return: integee
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
